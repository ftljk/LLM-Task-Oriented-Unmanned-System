# 硕士学位论文中期报告（草稿）

题目：基于大语言模型结合任务依赖的无人系统协作技术研究与应用验证

---

## 1. 学术研究进度情况

本课题自开题以来，按照计划稳步推进，已完成以下主要工作：

**第一阶段（2025.09-2025.11）**：完成了基于依赖感知的任务分解算法原型设计。基于 Go 语言和 Eino 大模型应用开发框架，实现了自然语言指令到结构化任务计划的转换管道，包括中文别名预处理、LLM 任务规划 Agent、以及计划验证与修复模块。

**第二阶段（2025.11-2026.01）**：完成了系统核心模块的开发与集成。实现了基于有向无环图（DAG）的多机器人任务调度器，支持依赖解析、并行执行、超时重试和故障跳过等机制；设计了 RobotAdapter 接口抽象，同时支持纯 Go 仿真引擎和 ROS2/Gazebo 真实仿真两种运行模式。

**第三阶段（2026.01-2026.02）**：实现了多机器人协作控制与调度算法。调度器支持并发 goroutine 执行无依赖任务、死锁检测、基于 TaskConfig 的可配置重试策略和 SkipAndContinue 故障隔离机制。

**第四阶段（2026.02-2026.06）**：构建了完整的仿真实验环境。完成了 ROS2/Gazebo 集成，包括差速驱动 URDF 模型设计、WebSocket 桥接通信、C++ world_pose_bridge 节点开发、以及自定义物理步长仿真世界配置；解决了 URDF 轮子朝向、底盘碰撞、速度精度等多个集成调试问题，目前系统已能够在 Gazebo 环境中稳定执行多机器人移动任务。

当前进度与开题计划基本吻合，处于仿真环境验证与中期报告撰写阶段。

---

## 2. 已完成的研究内容和取得的研究结果

### 2.1 系统总体架构设计

本研究设计并实现了一套从自然语言指令到多机器人执行控制的全链路系统。系统采用分层架构设计，自顶向下共五层：

**（1）指令预处理层（Normalizer）**：负责中文自然语言指令的归一化处理。包括机器人别名映射（如"一号"、"二号"、"car1"等 14 种别名统一转为 robot1/robot2）、基于正则表达式的动作提示提取（旋转角度、移动距离和方向）、以及提示去重与目标关联。

**（2）大语言模型规划层（LLM Agent）**：基于 Eino ADK 框架构建的 TaskPlanner Agent，通过 DeepSeek-v4-flash 模型（Volcengine Ark 平台）进行任务分解。Agent 配备三个工具：SubmitPlan（提交结构化任务计划）、SetRobotPosition（设置机器人绝对位置）、ComputeMotionParams（计算目标导向的运动参数）。提示工程包含差速驱动约束说明、三个中文示例和参数使用指南。

**（3）计划验证与修复层（Validator）**：对 LLM 输出的任务计划进行两轮验证。第一轮基于预处理阶段的提示匹配进行语义对齐和展开（如将单步 Move 自动展开为 Move+Wait+Stop 三段式）；第二轮作为安全网，为所有未匹配的持续运动任务添加默认超时停止，防止机器人在指令丢失时无限运动。

**（4）DAG 调度执行层（Scheduler）**：基于有向无环图模型管理任务依赖关系。调度器持续扫描待执行任务集合，识别所有依赖已满足的"就绪任务"，通过 Goroutine 并发执行无依赖任务；支持死锁检测、可配置重试（最多 3 次、间隔 1 秒）、任务超时（默认 30 秒）和故障策略（SkipAndContinue 使单任务失败不影响后续任务）。

**（5）机器人适配层（RobotAdapter）**：通过接口抽象支持两种运行模式——纯 Go 仿真模式（GOSimRobotAdapter）使用 10Hz 的 Goroutine 物理循环，不依赖任何外部服务，适合快速开发和单元测试；ROS2/Gazebo 仿真模式（ROS2RobotAdapter）通过 WebSocket 桥接通信，支持真实的物理仿真环境。

### 2.2 任务分解与依赖建模方法

#### 2.2.1 DAG 任务模型

系统定义了结构化的任务模型 TaskPlan → Task → TaskConfig，其中 TaskPlan 包含一组带有依赖关系的子任务。每个子任务包含动作类型（Move/CollectData/Wait）、目标机器人、参数映射、依赖列表、执行配置（超时、重试、故障策略）和运行时状态。

依赖关系通过 DAG 的有向边表示。例如，任务序列"先移动到(3,1)，再回到原点"被分解为：

```
task-1 (Move: robot1, x=0.5)
  └→ task-2 (Wait: duration=6.0)  ← 依赖 task-1
       └→ task-3 (Move: robot1, x=0)  ← 依赖 task-2
            └→ task-4 (Wait: duration=10.0)  ← 依赖 task-3
                 └→ task-5 (Move: robot1, x=0)  ← 依赖 task-4
```

无依赖关系的任务并行执行；有依赖关系的任务严格按照拓扑顺序执行。

#### 2.2.2 LLM 任务分解能力

LLM Agent 通过精心设计的提示工程实现任务分解。提示包含三部分：基础指令（动作类型定义、工具使用说明）、差速驱动约束（仅支持 x 轴前后移动和 z 轴原地旋转，不支持 y 轴横向移动）、以及三个中文示例（单机定时移动、双机并行+旋转、带位置计算的多机协同）。

Agent 的 ComputeMotionParams 工具计算目标位置运动参数：给定目标坐标和速度，计算需要旋转的角度（使机器人朝向目标方向）和前进距离，返回结构化的 JSON 数据供 LLM 规划。这解决了 LLM 难以直接计算几何角度的问题。

#### 2.2.3 计划验证与安全机制

Validator 模块实现了两轮验证：

**第一轮：Hints 驱动的展开**。预处理阶段提取的动作提示（如"旋转90度"、"前进1米"）与 LLM 输出的 Move 任务按目标机器人对齐。若 Move 任务没有对应的 Wait+Stop 依赖，系统自动插入三段式执行序列：Move（设置速度）→ Wait（等待指定时间）→ Move（速度归零停止）。旋转任务使用角速度 1.0 rad/s，移动任务使用线速度 0.5 m/s。

**第二轮：安全网**。遍历所有 Move 任务，对任何设置了非零速度但未被展开的任务，自动插入默认定时（5 秒）的等待和停止任务。这防止了因 LLM 输出不完整导致的机器人无限运动。

### 2.3 多机器人调度执行算法

调度器采用事件驱动循环设计，核心算法流程如下：

1. 构建任务 ID 到 Task 的映射表，初始化状态为 Pending
2. 循环扫描所有任务状态，识别 Ready 任务（全部依赖已完成）
3. 并行启动所有 Ready 任务（go routine 并发）
4. 等待当前批次完成，重新扫描下一批
5. 若无可就绪任务且无运行中任务，检查死锁；否则继续等待
6. 所有任务完成时结束

**依赖传播**：当某任务因依赖失败而被跳过时（如任务 B 依赖 A，A 失败→B 自动 Skipped），跳过标记沿依赖链向下传播。这通过 SkipAndContinue 策略实现，避免单点故障阻塞整个计划。

**死锁检测**：当无 Ready 任务且无 Running 任务但存在 Pending 任务时，调度器检测为死锁并返回错误。

**并发安全**：所有 RobotAdapter 调用由调度器管理，通过 Mutex 保护共享状态。WebSocket 客户端采用单 reader 设计，避免 goroutine 竞态。

### 2.4 仿真环境与系统集成

#### 2.4.1 双模拟器架构

系统支持两种仿真模式，通过 `--simulator` 命令行参数切换：

**GoSim 模式**：纯 Go 实现的内置仿真器。每个机器人维护位置 (x, y, theta) 和速度 (vx, vy, vz) 状态，后台 Goroutine 以 10Hz 频率更新物理位置，使用中点法积分。该模式无需任何外部依赖，启动即用。

**ROS2 模式**：连接外部 ROS2/Gazebo 仿真环境。通过 WebSocket 桥接（ros_bridge）与运行中的 ROS2 系统通信。适配器实现 SetVelocity（发布 Twist 到 cmd_vel topic）、GetOdometry（订阅 Odometry topic，单次订阅+等待模式，7 秒超时，支持四元数到偏航角的转换）。

#### 2.4.2 ROS2/Gazebo 集成

为支持真实物理仿真，完成了完整的 ROS2/Gazebo 工具链集成：

**URDF 机器人模型**：设计了双差速驱动机器人模型，采用 ROS2 diff_drive_controller 插件。包含 base_link、base_footprint、左右驱动轮和万向轮。经过多轮调试，最终采用 `axis xyz="0 0 1"` 和 `rpy="-1.5708 0 0"` 的轮子配置（Gazebo 在 child frame 中解释 joint axis），并调整了 base_joint 高度（z=0.10 提供 2.5cm 离地间隙）以防止底盘摩擦地面。

**C++ world_pose_bridge 节点**：开发了 C++ gz-transport 节点，直接订阅 Gazebo 的 `/world/empty/pose/info` topic 获取机器人世界坐标，并作为 ROS2 节点发布 `/world_pose/robot{1,2}` PoseStamped 消息。同时转发 cmd_vel 请求到 Gazebo 作为辅助控制通道，弥补 ros_gz_bridge 可能的不稳定。

**Python 桥接节点**：实现了 ROS2 Python 桥接节点，连接 ROS2 与 Go Agent 之间的 WebSocket 通信。负责订阅 `/world_pose/robot{1,2}`（获取世界坐标）、`/model/robot{1,2}/odometry`（获取里程计）等信息，并提供 cmd_vel 的 WebSocket 接口。

**自定义仿真世界**：创建了 `empty_5ms.sdf` 世界文件，将物理仿真步长从默认的 1ms 调整为 5ms，以解决速度精度偏低问题（实际速度约为指令值的 1/5，分析原因为默认 1ms 步长导致 real_time_factor < 1.0）。

### 2.5 实验验证

（待实验数据填充——此部分将在完成四个实验场景后补充，包括单机单步移动、单机顺序依赖、双机并行和故障恢复场景的成功率与精度统计。）

### 2.6 与传统方法的对比分析

与 DART-LLM 等同类工作相比，本系统在以下方面具有特色：

- **Go 语言全栈实现**：基于 Go 的高并发特性，相比 Python 方案在多机器人并行调度方面具有天然优势
- **双模拟器架构**：GoSim 模式支持离线开发测试，ROS2 模式提供真实物理验证，两套模式共享同一接口抽象
- **三层 Normalizer**：预处理→LLM→验证的三层管道设计相比单次 LLM 分解具有更强的可靠性和可调试性
- **用户交互细节**：中文别名支持、实时状态显示、会话管理等功能提升了使用体验

---

## 3. 目前存在的或预期可能出现的问题

### 3.1 速度精度偏差

在 Gazebo 仿真中，机器人的实际移动速度和旋转速度与指令值存在约 5 倍的偏差（实际约为指令的 1/5）。初步分析认为这与物理仿真步长的 real_time_factor 有关。已通过自定义 5ms 步长世界文件进行优化，但仍需定量验证其效果。

### 3.2 SetPosition 未实现

ROS2 模式下 `SetPosition`（遥置机器人位置）未真正实现。目前 Gazebo 端不支持通过话题直接设置模型位姿，需要通过 Gazebo 的 `world/set_pose` 服务或移除并重新生成模型来实现。当前该接口仅打印日志并返回 nil，尚未实现功能。

### 3.3 缺乏视觉感知能力

开题计划中提到的多模态感知（视觉-语言模型）尚未实现。当前的系统仅支持基于自然语言的位置/速度指令，无法处理包含视觉信息的任务（如"检测前方障碍物"、"识别目标物体"）。

### 3.4 Gazebo 物理稳定性

在 Gazebo 仿真中，差速驱动机器人存在轮子碰撞不稳定现象。虽然通过调整 URDF 的 base_joint 高度、移除 base_footprint collision、添加摩擦面等措施已基本解决，但在高速旋转或长时间运行时仍有轻微抖动，需进一步优化。

### 3.5 真实机器人部署

当前所有工作均在仿真环境中完成，尚未在真实机器人硬件上进行部署验证。从仿真到真实环境的迁移可能面临传感器噪声、通信延迟、物理参数差异等诸多挑战。

### 3.6 任务分解范围有限

系统的任务分解能力目前集中在移动操作（移动到位置、旋转、等待）和数据采集三个方面。开题计划中提到的机械臂操作、无人机的三维运动、以及"搜索-标记-抓取"等多机器人协同任务链尚未实现。

---

## 4. 后期学术研究或实践工作的进度安排

| 起始时间 | 完成时间 | 计划工作内容 |
|---------|---------|------------|
| 2026.06 | 2026.07 | 完成实验评估：4个场景各10-20次，收集成功率、位置精度、执行时间等指标；录制演示视频；完成中期报告 |
| 2026.07 | 2026.08 | 优化提示信息，完成安全网机制，修复速度精度问题；实现 ROS2 SetPosition（通过 Gazebo service 接口） |
| 2026.08 | 2026.09 | 引入视觉感知能力，集成多模态大模型，支持"检测到物体后移动到该位置"等复合任务 |
| 2026.09 | 2026.10 | 探索异构机器人（无人机）的集成能力，扩展 DAG 任务模型支持三维空间运动 |
| 2026.10 | 2026.11 | 对比实验：与 DART-LLM、传统预编程方法和行为树方法在相同场景下比较成功率、执行效率和用户交互体验 |
| 2026.11 | 2026.12 | 完成学位论文初稿撰写 |
| 2026.12 | 2026.01 | 学位论文修改与答辩准备 |

其中 2026.06-2026.07 的中期报告相关工作为当前阶段的优先任务。
